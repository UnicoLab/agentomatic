# Prometheus scrape configuration for Agentomatic.
#
# Assumes your Agentomatic app exposes /metrics (enable_metrics=True).
# When running the app on the host (outside docker), Prometheus reaches it
# via host.docker.internal. Adjust targets for your environment.

global:
  scrape_interval: 15s
  evaluation_interval: 15s

scrape_configs:
  - job_name: agentomatic
    metrics_path: /metrics
    static_configs:
      - targets:
          # The repository's packaged Docker platform publishes 8010 by
          # default. Change this to 8000 only when Agentomatic itself runs
          # directly on the host with that port.
          - "host.docker.internal:8010"
        labels:
          service: agentomatic

  - job_name: otel-collector
    static_configs:
      - targets:
          - "otel-collector:8888"

  - job_name: prometheus
    static_configs:
      - targets:
          - "localhost:9090"
